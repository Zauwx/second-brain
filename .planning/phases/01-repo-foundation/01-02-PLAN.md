---
phase: 01-repo-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - pyproject.toml
  - app/__init__.py
  - app/main.py
  - app/core/__init__.py
  - app/core/config.py
  - app/api/__init__.py
  - app/api/health.py
  - app/notes/__init__.py
  - app/services/__init__.py
  - app/repositories/__init__.py
autonomous: true
requirements: [OPS-05]

must_haves:
  truths:
    - "An importable FastAPI app exists at app.main:app with a registered health router"
    - "GET /health returns 200 with JSON {\"status\": \"ok\"}"
    - "uv run ruff check app/ passes with zero errors on the skeleton"
    - "Settings load from environment via pydantic-settings BaseSettings in app/core/config.py"
    - "The domain-per-folder layout is seeded so Phase 2 slots in cleanly"
    - "D-07: structured domain-per-folder layout (app/core, app/api with health router, app/main.py, empty __init__.py placeholders for notes/services/repositories)"
    - "D-08: pyproject.toml managed by uv with ruff + mypy configured; uv run ruff check app/ passes clean"
    - "D-09: GET /health returns JSON {\"status\": \"ok\"} with 200"
    - "D-11: settings loaded via pydantic-settings BaseSettings in app/core"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv-managed project metadata + deps + ruff + mypy config"
      contains: "[tool.ruff]"
    - path: "app/main.py"
      provides: "FastAPI app factory assembly + router registration"
      contains: "FastAPI"
    - path: "app/api/health.py"
      provides: "health APIRouter with GET /health returning {status: ok}"
      contains: "/health"
    - path: "app/core/config.py"
      provides: "pydantic-settings BaseSettings Settings class"
      contains: "BaseSettings"
  key_links:
    - from: "app/main.py"
      to: "app/api/health.py"
      via: "include_router"
      pattern: "include_router"
    - from: "app/core/config.py"
      to: "pydantic_settings"
      via: "BaseSettings import"
      pattern: "from pydantic_settings import"
---

<objective>
Scaffold the uv-managed Python project and a structured FastAPI skeleton that builds, lints clean, and exposes a real `GET /health` endpoint — the smoke test for "the toolchain works." Seed the domain-per-folder layout so Phase 2 (notes/MySQL) slots in without restructuring.

Purpose: Satisfy ROADMAP success criterion 4 (`uv run ruff check app/` passes) and provide the runnable app that Plan 03 containerizes to satisfy criterion 3.
Output: `pyproject.toml` + `app/` package (core/config, api/health, empty domain placeholders).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-foundation/01-CONTEXT.md
@.planning/phases/01-repo-foundation/SKELETON.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md

<interfaces>
<!-- Contracts this plan establishes for downstream phases. Executor creates these; no exploration needed. -->

Health endpoint contract (consumed by Plan 03 Docker healthcheck + README quickstart):
  GET /health  ->  200  {"status": "ok"}

Settings contract (app/core/config.py) — pydantic-settings BaseSettings, reads from .env:
  class Settings(BaseSettings):
      # Phase 1 needs almost nothing at runtime, but the class must load cleanly.
      # Use model_config = SettingsConfigDict(env_file=".env", extra="ignore")
      # so the rich .env.example variables (MySQL/JWT/Anthropic/Ollama) do not
      # raise validation errors before later phases consume them.
      ...

App factory contract (app/main.py):
  app: FastAPI  with health router registered via app.include_router(...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initialize uv project with ruff + mypy config and FastAPI deps</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/01-repo-foundation/01-CONTEXT.md (D-08 uv + ruff + mypy in pyproject.toml)
    - .planning/research/STACK.md (Development Tools table: uv, ruff >=0.8.0, mypy >=1.13.0 py3.12; FastAPI 0.115.x, uvicorn 0.32.x, pydantic-settings 2.x; Installation section uv commands)
    - CLAUDE.md (recommended versions; "What NOT to Use" — do NOT add passlib/python-jose/aiomysql)
    - .gitignore (confirm .venv/ and caches are ignored before running uv which creates .venv)
  </read_first>
  <behavior>
    - `uv sync` resolves and installs without error
    - `uv run python -c "import fastapi, uvicorn, pydantic_settings"` succeeds (deps present)
    - `[tool.ruff]` and `[tool.mypy]` sections exist in pyproject.toml
    - `uv run ruff check .` runs (zero errors expected once app code lands in Task 2)
  </behavior>
  <action>
    Initialize the project with `uv` so it generates `pyproject.toml` (and `uv.lock`). Require Python 3.12. Add Phase-1 runtime deps only: `fastapi` (0.115.x), `uvicorn[standard]` (0.32.x), `pydantic-settings` (2.x). Add dev deps: `ruff` (>=0.8.0), `mypy` (>=1.13.0), `pytest`, `pytest-asyncio`, `httpx` (for the health test). Do NOT add MySQL/Alembic/Ollama/Anthropic deps yet — those arrive in their phases (keep the skeleton thin per Walking Skeleton rules).
    Configure tooling inside `pyproject.toml`: a `[tool.ruff]` section (target py312, a reasonable lint+format rule set — start sensible, can tighten later per D "Claude's Discretion"); a `[tool.mypy]` section with `python_version = "3.12"` (strictness reasonable to start); and `[tool.pytest.ini_options]` with `asyncio_mode = "auto"`. Pin the project to require-python 3.12.
  </action>
  <verify>
    <automated>cd "F:/My-home-lab" &amp;&amp; uv sync &amp;&amp; uv run python -c "import fastapi, uvicorn, pydantic_settings; print('deps ok')" &amp;&amp; grep -q '\[tool.ruff\]' pyproject.toml &amp;&amp; grep -q '\[tool.mypy\]' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `uv sync` exits 0 and creates `uv.lock`
    - `pyproject.toml` requires Python 3.12 and lists fastapi, uvicorn, pydantic-settings as runtime deps
    - `pyproject.toml` contains `[tool.ruff]`, `[tool.mypy]` (python_version = "3.12"), and pytest asyncio_mode = "auto"
    - No forbidden libs present: `grep -Eiq 'passlib|python-jose|aiomysql' pyproject.toml` returns nothing
  </acceptance_criteria>
  <done>uv project initialized; deps installed; ruff/mypy/pytest configured in pyproject.toml; lockfile generated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the FastAPI skeleton — config, health router, app assembly, domain placeholders</name>
  <files>app/__init__.py, app/main.py, app/core/__init__.py, app/core/config.py, app/api/__init__.py, app/api/health.py, app/notes/__init__.py, app/services/__init__.py, app/repositories/__init__.py</files>
  <read_first>
    - .planning/phases/01-repo-foundation/01-CONTEXT.md (D-07 layout: app/core settings, app/api health router, app/main.py, empty __init__.py placeholders for notes/services/repositories; D-09 /health returns {"status":"ok"}; D-11 pydantic-settings in app/core)
    - .planning/research/ARCHITECTURE.md (domain-per-folder rationale; lifespan pattern; Router pattern — routers hold zero business logic)
    - .planning/research/STACK.md (FastAPI lifespan example; pydantic-settings BaseSettings example)
    - pyproject.toml (confirm dep names available)
  </read_first>
  <behavior>
    - GET /health returns HTTP 200 and JSON body exactly {"status": "ok"}
    - app.main:app imports without raising (Settings loads with extra env vars ignored)
    - Swagger UI is served at /docs (FastAPI default) — confirmed reachable in Plan 03 via Docker
    - ruff reports zero errors on app/
  </behavior>
  <action>
    Create the package layout under `app/` (D-07):
    - `app/core/config.py`: a `Settings(BaseSettings)` class using pydantic-settings. Configure `model_config = SettingsConfigDict(env_file=".env", extra="ignore")` so the rich `.env.example`/`.env` variables for later phases do not raise validation errors now. Phase 1 needs no required runtime settings; keep fields minimal/optional (e.g. an `environment: str = "development"`). Expose a module-level `settings = Settings()` instance.
    - `app/api/health.py`: an `APIRouter` defining `GET /health` that returns `{"status": "ok"}` with a 200 status. No business logic, no DB.
    - `app/main.py`: build the FastAPI app (title "Second Brain"), register the health router via `app.include_router(...)`, and expose `app` at module level so `uvicorn app.main:app` works. A `lifespan` async context manager is optional this phase (no resources to open yet) — keep it minimal or omit.
    - Create empty `__init__.py` placeholder files so the domain-per-folder layout is seeded but empty: `app/__init__.py`, `app/core/__init__.py`, `app/api/__init__.py`, `app/notes/__init__.py`, `app/services/__init__.py`, `app/repositories/__init__.py`. These are intentionally empty placeholders for Phase 2 — do NOT add models/CRUD now.
    Ensure all code passes ruff (formatting + lint) and is type-clean enough for mypy at the configured strictness.
  </action>
  <verify>
    <automated>cd "F:/My-home-lab" &amp;&amp; uv run ruff check app/ &amp;&amp; uv run python -c "from app.main import app; print([r.path for r in app.routes])" | grep -q '/health'</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check app/` passes with ZERO errors (ROADMAP success criterion 4)
    - `from app.main import app` succeeds without raising
    - The app's route table includes `/health`
    - `app/notes/__init__.py`, `app/services/__init__.py`, `app/repositories/__init__.py` exist and are empty placeholders (no CRUD/models)
    - `app/core/config.py` imports `BaseSettings` from `pydantic_settings` and sets `extra="ignore"`
  </acceptance_criteria>
  <done>The FastAPI skeleton imports cleanly, exposes GET /health returning {"status":"ok"}, passes ruff, and seeds the domain folder layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the health endpoint test (local app-level smoke test via httpx)</name>
  <files>tests/__init__.py, tests/test_health.py</files>
  <read_first>
    - .planning/research/STACK.md (httpx AsyncClient + ASGITransport testing pattern; pytest-asyncio asyncio_mode auto)
    - app/main.py (the app object to mount in the test transport)
    - app/api/health.py (the expected 200 + {"status":"ok"} contract)
  </read_first>
  <behavior>
    - Test calls GET /health through ASGITransport against app.main:app
    - Asserts status_code == 200
    - Asserts response JSON == {"status": "ok"}
  </behavior>
  <action>
    Create `tests/__init__.py` (empty) and `tests/test_health.py` containing one async test that mounts `app.main:app` via `httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://test")`, issues `GET /health`, and asserts the status code is 200 and the JSON body equals `{"status": "ok"}`. This is the app-level proof of the contract before Docker proves it end-to-end in Plan 03. Rely on `asyncio_mode = "auto"` from pyproject.toml (no per-test decorator needed). Keep `tests/` lint-clean too if ruff is configured to include it.
  </action>
  <verify>
    <automated>cd "F:/My-home-lab" &amp;&amp; uv run pytest tests/test_health.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_health.py` passes (1 test, 0 failures)
    - The test asserts both the 200 status code and the exact `{"status": "ok"}` body
    - No live server is required (ASGITransport in-process)
  </acceptance_criteria>
  <done>A passing httpx-based test proves GET /health returns 200 {"status":"ok"} at the app level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment variables -> application config | Untyped env input is read into Settings; unexpected/extra vars must not crash the app |
| uv-created .venv / caches -> git | Generated artifacts must stay out of version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial of Service | app.main:app fails to start because Settings rejects the rich .env.example vars for unbuilt phases | mitigate | `SettingsConfigDict(extra="ignore")` in app/core/config.py (Task 2) so later-phase vars do not break Phase-1 startup |
| T-01-05 | Information Disclosure | uv `.venv/` or caches committed, potentially leaking local paths/state | mitigate | `.gitignore` (Plan 01) already excludes `.venv/`, `.ruff_cache/`, `.mypy_cache/`, `.pytest_cache/`; depends_on 01-01 guarantees ordering |
| T-01-SC | Tampering | Python package installs via uv (supply chain) | accept | All Phase-1 deps (fastapi, uvicorn, pydantic-settings, ruff, mypy, pytest, httpx) are well-known, high-reputation PyPI packages vetted in STACK.md/CLAUDE.md; no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- `uv sync` succeeds; `uv.lock` exists
- `uv run ruff check app/` passes with zero errors (ROADMAP criterion 4)
- `from app.main import app` imports; `/health` present in routes
- `uv run pytest tests/test_health.py` passes asserting 200 + {"status":"ok"}
- No forbidden libs (passlib/python-jose/aiomysql) in pyproject.toml
</verification>

<success_criteria>
A uv-managed, ruff-clean FastAPI skeleton exists with a real `GET /health` endpoint returning `{"status":"ok"}`, settings loaded via pydantic-settings, the domain-per-folder layout seeded with empty placeholders, and a passing app-level health test. The app is ready to be containerized in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/01-repo-foundation/01-02-SUMMARY.md` when done.
</output>
