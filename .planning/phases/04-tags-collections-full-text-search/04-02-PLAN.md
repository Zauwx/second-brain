---
phase: 04-tags-collections-full-text-search
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/test_notes_tag_filter.py
  - app/notes/repository.py
  - app/notes/router.py
  - app/notes/service.py
autonomous: true
requirements: [ORG-02]
user_setup: []

must_haves:
  truths:
    - "GET /notes?tag=python returns only the caller's notes carrying that tag"
    - "GET /notes?tag=python&tag=docker returns only notes carrying BOTH tags (AND-intersection)"
    - "Tag filter input is normalized (?tag=Python matches stored 'python')"
    - "Filtered note items include their tags array with no N+1 (selectinload)"
    - "Tag filter composes with existing user_id scoping, pagination, sort, and content filter"
  artifacts:
    - path: "app/notes/repository.py"
      provides: "list_paginated tags AND-intersection filter"
      contains: "having"
    - path: "tests/test_notes_tag_filter.py"
      provides: "ORG-02 single + multi-tag AND coverage"
      min_lines: 40
  key_links:
    - from: "app/notes/router.py"
      to: "list_paginated"
      via: "tag query param threaded through service"
      pattern: "tag.*Query"
    - from: "app/notes/repository.py"
      to: "note_tags + Tag"
      via: "GROUP BY HAVING COUNT(DISTINCT) subquery"
      pattern: "count\\(.*distinct"
---

<objective>
Deliver ORG-02: filtering the existing `GET /notes` list by one or more tags with AND (intersection) semantics, composing with the Phase-3 user_id scoping and the existing pagination/sort/content-filter contract. Builds directly on the `Note.tags` relationship, `selectinload`, and the `Tag` model from Plan 01.

Purpose: Tag filtering is the payoff of the tags slice — it makes tags useful for retrieval.
Output: extended `NoteRepository.list_paginated` + `GET /notes` query param, and green `tests/test_notes_tag_filter.py`.
</objective>

<phase_goal>
**As a** Second Brain user, **I want to** filter my notes by one or more tags, **so that** I can narrow my knowledge base to a topic (and require several tags at once).

(Derived 1:1 from locked requirement ORG-02.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tags-collections-full-text-search/04-CONTEXT.md
@.planning/phases/04-tags-collections-full-text-search/04-RESEARCH.md
@.planning/phases/04-tags-collections-full-text-search/04-PATTERNS.md
@.planning/phases/04-tags-collections-full-text-search/04-01-SUMMARY.md

<interfaces>
From app/notes/repository.py (after Plan 01):
- list_paginated(page, size, sort="-created_at", filter=None, *, user_id) -> tuple[list[Note], int]; query already has `.options(selectinload(Note.tags))` and `.where(Note.user_id == user_id)`.
- module-level `note_tags` Table is importable from app/notes/models.py.

From app/tags/models.py: `Tag` (id, user_id, name normalized lowercase).

From app/notes/router.py: GET /notes/ list endpoint with page/size/sort/filter Query params calling the note service.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing tests/test_notes_tag_filter.py</name>
  <files>tests/test_notes_tag_filter.py</files>
  <read_first>
    - tests/test_notes_list.py (filter + pagination assertion patterns, lines 27-50, 216-233)
    - tests/test_tags.py (attach-tag helper pattern from Plan 01)
    - .planning/phases/04-tags-collections-full-text-search/04-PATTERNS.md (test_notes_tag_filter.py section, lines 934-967)
  </read_first>
  <action>
    Create `tests/test_notes_tag_filter.py` using the existing auth_client fixture. Tests (currently FAIL — filter not implemented): single-tag filter `GET /notes/?tag=python` returns the python-tagged note and excludes others; multi-tag AND `GET /notes/?tag=python&tag=docker` returns only the note carrying BOTH and excludes a note carrying only python (strict intersection per D-05); normalization `?tag=Python` matches a note stored with tag "python" (Pitfall 6); and that returned items include a populated `tags` array (proves selectinload / no N+1). Seed notes and attach tags via the Plan-01 endpoints.
  </action>
  <verify>
    <automated>uv run pytest tests/test_notes_tag_filter.py -q 2>&1 | grep -E "failed|passed|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - File defines `test_single_tag_filter`, `test_multi_tag_and_filter`, a normalization test, and an items-include-tags test
    - `test_multi_tag_and_filter` asserts the python-only note is NOT in results
    - Tests collect and are red (filter param not yet present)
  </acceptance_criteria>
  <done>test_notes_tag_filter.py exists, collects, and is red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AND-intersection tag filter on GET /notes</name>
  <files>app/notes/repository.py, app/notes/router.py, app/notes/service.py</files>
  <read_first>
    - app/notes/repository.py (list_paginated current body incl. filter block ~103-110 and count/pagination)
    - app/notes/router.py (GET /notes/ list endpoint + Query params)
    - app/notes/service.py (list method that calls list_paginated and builds NoteListResponse)
    - .planning/phases/04-tags-collections-full-text-search/04-RESEARCH.md (Pattern 3 AND-intersection; Pitfall 5 ordering/pagination; Pitfall 6 normalization)
    - .planning/phases/04-tags-collections-full-text-search/04-PATTERNS.md (notes/repository.py MODIFY, lines 621-668)
  </read_first>
  <behavior>
    - list_paginated(tags=["python"]) returns only notes (owner-scoped) having tag "python".
    - list_paginated(tags=["python","docker"]) returns only notes having BOTH (HAVING COUNT(DISTINCT)=2).
    - tags input is normalized strip().lower() before comparison.
    - ORDER BY / OFFSET / LIMIT applied on the OUTER Note query (not the subquery) so pagination is stable.
  </behavior>
  <action>
    Extend `NoteRepository.list_paginated` with a keyword-or-positional `tags: list[str] | None = None` parameter (before the keyword-only `*, user_id`). When `tags` is truthy, normalize to `[t.strip().lower() for t in tags]`, build the tag-id scalar subquery (`select(Tag.id).where(Tag.name.in_(normalized)).where(Tag.user_id == user_id)`), then the note-id subquery over `note_tags` grouping by note_id with `having(func.count(note_tags.c.tag_id.distinct()) == len(normalized))`, and apply `query = query.where(Note.id.in_(matching_note_ids))`. Import `note_tags` from app.notes.models and `Tag` locally (function-level import) to avoid a module-level circular import. Keep the existing `selectinload(Note.tags)`, count, sort, and pagination on the outer query unchanged (Pitfall 5). Do NOT add a `text()` or f-string SQL.
    Thread the param through the service `list` method and add `tag: list[str] | None = Query(default=None)` to the `GET /notes/` router endpoint, passing it as `tags=tag` to the service.
  </action>
  <verify>
    <automated>uv run pytest tests/test_notes_tag_filter.py tests/test_notes_list.py -q 2>&1 | tail -5 && uv run ruff check app/notes && uv run mypy app/notes</automated>
  </verify>
  <acceptance_criteria>
    - `app/notes/repository.py` list_paginated has a `tags` param and uses `func.count(...distinct())` + `.having(...)` + `Note.id.in_(`
    - `GET /notes/?tag=python&tag=docker` returns only notes with BOTH tags (200)
    - `GET /notes/?tag=Python` matches a note tagged "python" (normalization)
    - `uv run pytest tests/test_notes_tag_filter.py` exits 0 and `tests/test_notes_list.py` still green
    - ruff + mypy clean on app/notes
  </acceptance_criteria>
  <done>ORG-02 working: single and multi-tag AND filtering on GET /notes, normalized, no N+1, existing list behavior intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /notes | Untrusted repeated `tag` query params cross here |
| API → MySQL | Parameterized subquery; user_id scoping enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-iso | Elevation of Privilege | GET /notes?tag= | mitigate | Tag-id subquery filters `Tag.user_id == user_id` and outer query keeps `Note.user_id == user_id`; cross-user tags cannot match |
| T-04-04 | Tampering | tag filter SQL | mitigate | ORM `in_()`/`having()` parameterize values; no text()/f-string interpolation |
| T-04-05 | Denial of Service | many repeated tag params | accept | Single GROUP BY/HAVING pass (not N EXISTS); pagination caps result size; personal-scale dataset |
| T-04-SC | Tampering | dependency installs | accept | No new packages this phase |
</threat_model>

<verification>
- `uv run pytest tests/test_notes_tag_filter.py tests/test_notes_list.py -q` green
- `uv run ruff check app/notes` and `uv run mypy app/notes` clean
- Manual (Swagger): tag two notes, GET /notes/?tag=python&tag=docker returns only the doubly-tagged note
</verification>

<success_criteria>
ORG-02 met: GET /notes supports single and multiple `?tag=` params with AND-intersection semantics, normalized input, user-scoped, paginated, sorted, with tags eager-loaded.
</success_criteria>

<output>
Create `.planning/phases/04-tags-collections-full-text-search/04-02-SUMMARY.md` when done.
</output>
