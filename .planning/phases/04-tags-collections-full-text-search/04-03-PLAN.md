---
phase: 04-tags-collections-full-text-search
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/test_collections.py
  - alembic/versions/0005_add_collections.py
  - app/collections/__init__.py
  - app/collections/models.py
  - app/collections/schemas.py
  - app/collections/repository.py
  - app/collections/service.py
  - app/collections/router.py
  - app/notes/models.py
  - app/main.py
  - app/core/dependencies.py
autonomous: true
requirements: [ORG-03, ORG-04]
user_setup: []

must_haves:
  decisions: "Covers D-06, D-07, D-08 (collections many-to-many, per-user, REST surface)."
  truths:
    - "User can create a collection (POST /collections) and list their collections (GET /collections)"
    - "User can add a note to a collection (POST /collections/{id}/notes) and remove it (DELETE .../{note_id})"
    - "GET /collections/{id}/notes returns the collection's notes in the NoteListResponse envelope"
    - "Collections are per-user: accessing another user's collection returns 403 (exists) or 404 (missing)"
    - "A note can belong to multiple collections (many-to-many)"
  artifacts:
    - path: "app/collections/models.py"
      provides: "Collection ORM model, per-user, UNIQUE(user_id, name)"
      contains: "class Collection(Base)"
    - path: "app/collections/router.py"
      provides: "POST/GET /collections, POST/DELETE/GET /collections/{id}/notes"
      exports: ["router"]
    - path: "alembic/versions/0005_add_collections.py"
      provides: "collections + note_collections tables"
      contains: "note_collections"
    - path: "tests/test_collections.py"
      provides: "ORG-03/04 coverage incl. cross-user isolation"
      min_lines: 70
  key_links:
    - from: "app/notes/models.py"
      to: "note_collections / collections"
      via: "relationship(secondary=note_collections)"
      pattern: "relationship\\(\\s*\"Collection\""
    - from: "app/collections/repository.py"
      to: "Note via note_collections"
      via: "join + selectinload(Note.tags) returning (list, total)"
      pattern: "selectinload\\(Note.tags\\)"
    - from: "app/collections/service.py"
      to: "ownership"
      via: "get_or_404_owned 403/404"
      pattern: "403|404"
    - from: "app/main.py"
      to: "app/collections/router.py"
      via: "include_router prefix=/collections"
      pattern: "collections_router"
---

<objective>
Deliver the Collections vertical slice (ORG-03, ORG-04): a user can create collections, add/remove notes (many-to-many), list their collections, and list a collection's notes via the canonical `NoteListResponse` envelope — all per-user with the Phase-3 403/404 ownership contract. Builds on Plan 01's schema foundation (selectinload, Note model conventions).

Purpose: Collections are the second organization layer; they reuse the notes ownership + envelope patterns wholesale.
Output: `app/collections/` package, migration `0005`, `Note.collections_rel`, registered router, and green `tests/test_collections.py`.
</objective>

<phase_goal>
**As a** Second Brain user, **I want to** group my notes into named collections and list a collection's notes, **so that** I can organize the same note under multiple themed groupings.

(Derived from locked requirements ORG-03 and ORG-04.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tags-collections-full-text-search/04-CONTEXT.md
@.planning/phases/04-tags-collections-full-text-search/04-RESEARCH.md
@.planning/phases/04-tags-collections-full-text-search/04-PATTERNS.md
@.planning/phases/04-tags-collections-full-text-search/04-01-SUMMARY.md

<interfaces>
From app/notes/schemas.py:
- NoteListResponse{items: list[NoteRead], total, page, size, pages} — reuse verbatim for GET /collections/{id}/notes; do NOT duplicate.
- NoteRead now has `tags` field (Plan 01).

From app/notes/repository.py: NoteRepository(session); get_by_id(note_id) -> Note|None (selectinload present).

From app/notes/service.py: get_or_404_owned ownership pattern (404 None / 403 wrong owner).

From app/notes/models.py: module-level association Table style (see note_tags from Plan 01); Note model.

From app/core/dependencies.py: get_db, get_current_user, get_note_service pattern (50-54).

From app/main.py: include_router pattern; auth at /auth, notes at /notes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing tests/test_collections.py (incl. isolation)</name>
  <files>tests/test_collections.py</files>
  <read_first>
    - tests/test_notes_crud.py (create + assert structure, lines 27-64)
    - tests/test_notes_isolation.py (user_a_client/user_b_client cross-user 403/404 pattern, lines 48-134)
    - .planning/phases/04-tags-collections-full-text-search/04-PATTERNS.md (test_collections.py section 971-997)
  </read_first>
  <action>
    Create `tests/test_collections.py` (red until implemented). Cover: create collection `POST /collections/` returns 201 with id+name; add note `POST /collections/{id}/notes` {"note_id":N}; list `GET /collections/{id}/notes` returns the NoteListResponse envelope (items/total/pages) containing the added note; `GET /collections/` lists the caller's collections; remove note `DELETE /collections/{id}/notes/{note_id}` returns 204 and the note disappears from the listing; and isolation (`test_collection_isolation`): user B's `GET /collections/{A_id}/notes` returns 403, B's `POST /collections/{A_id}/notes` returns 403, and a missing collection id returns 404. Use auth_client for single-user and user_a_client/user_b_client for isolation.
  </action>
  <verify>
    <automated>uv run pytest tests/test_collections.py -q 2>&1 | grep -E "failed|passed|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - Defines create/add/list-notes/list-collections/remove tests + `test_collection_isolation`
    - Isolation test asserts 403 for cross-user access and 404 for missing collection
    - Tests collect and are red
  </acceptance_criteria>
  <done>test_collections.py exists, collects, red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema layer — migration 0005, Collection model, Note.collections_rel</name>
  <files>alembic/versions/0005_add_collections.py, app/collections/__init__.py, app/collections/models.py, app/notes/models.py</files>
  <read_first>
    - app/auth/models.py (User model analog)
    - app/notes/models.py (note_tags Table style from Plan 01; Note class; imports)
    - alembic/versions/0004_add_tags.py (Plan 01 migration — chain from here)
    - .planning/phases/04-tags-collections-full-text-search/04-RESEARCH.md (Pattern 1; Pattern 7 collections/note_collections; Collection model 805-825)
    - .planning/phases/04-tags-collections-full-text-search/04-PATTERNS.md (collections/models.py 265-289; migration 785-836)
  </read_first>
  <behavior>
    - After upgrade: `collections` table with UNIQUE(user_id, name); `note_collections` join with composite PK + ON DELETE CASCADE FKs.
    - `note.collections_rel` is accessible (relationship resolves; Collection model registered).
  </behavior>
  <action>
    Create migration `0005_add_collections` with `down_revision = "0004_add_tags"`. upgrade(): create `collections` (unsigned INT PK, user_id FK→users.id ON DELETE CASCADE indexed, name String(255) NOT NULL, UNIQUE(user_id, name) named uq_user_collection, InnoDB/utf8mb4) and `note_collections` (composite PK note_id+collection_id, FK→notes.id and FK→collections.id both ON DELETE CASCADE, InnoDB). downgrade() drops note_collections then collections.
    Create `app/collections/models.py` with `Collection(Base)` (tuple-form __table_args__: UniqueConstraint uq_user_collection + dict last; unsigned INT id, user_id FK indexed, name String(255)). Create empty `app/collections/__init__.py`.
    In `app/notes/models.py`: add the module-level `note_collections` Table (string FK refs "notes.id"/"collections.id", ON DELETE CASCADE, InnoDB) and add `collections_rel: Mapped[list["Collection"]] = relationship("Collection", secondary=note_collections, lazy="select")` to the Note class. Use `lazy="select"`.
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run python -c "import app.collections.models, app.notes.models" && uv run pytest tests/test_notes_crud.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `app/collections/models.py` contains `class Collection(Base)` and `UniqueConstraint("user_id", "name", name="uq_user_collection")`
    - `alembic/versions/0005_add_collections.py` has `down_revision = "0004_add_tags"` and creates `note_collections`
    - `app/notes/models.py` defines `note_collections` Table and `relationship("Collection", secondary=note_collections`
    - `uv run alembic upgrade head` succeeds and importing both model modules raises no mapper error
  </acceptance_criteria>
  <done>Migration applies; Collection model + note_collections + Note.collections_rel exist; mappers configure cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Collections API — repository/service/router + wiring; make test_collections.py green</name>
  <files>app/collections/schemas.py, app/collections/repository.py, app/collections/service.py, app/collections/router.py, app/main.py, app/core/dependencies.py</files>
  <read_first>
    - app/notes/repository.py (constructor, create, list_paginated count+page structure 63-122)
    - app/notes/service.py (get_or_404_owned 44-67; NoteListResponse envelope 69-112)
    - app/notes/router.py (router decl; Query params; 201 + 204 patterns)
    - app/core/dependencies.py (get_note_service 50-54)
    - app/main.py (router registration)
    - .planning/phases/04-tags-collections-full-text-search/04-PATTERNS.md (collections schemas 293-310; repository 314-371; service 375-400; router 404-454; dependencies 720-751; main.py 755-781)
    - tests/test_collections.py (the contract to satisfy)
  </read_first>
  <behavior>
    - POST /collections {"name":"Work"} → 201 CollectionRead (owner = caller).
    - GET /collections → list[CollectionRead] for the caller only.
    - POST /collections/{id}/notes {"note_id":N} → 204, both collection and note owned by caller (else 403/404).
    - DELETE /collections/{id}/notes/{note_id} → 204.
    - GET /collections/{id}/notes → NoteListResponse of the collection's notes (selectinload tags, paginated).
    - Cross-user collection access → 403 (exists) / 404 (missing).
  </behavior>
  <action>
    Create `app/collections/schemas.py` (CollectionCreate{name: str Field min_length=1 max_length=255}, CollectionRead{from_attributes; id, name, user_id}). Reuse NoteListResponse from app.notes.schemas for the notes listing — do not duplicate.
    Create `app/collections/repository.py`: CollectionRepository(session) with get_by_id, create(user_id, name) (add/commit/refresh), list_by_user(user_id), add_note(collection, note)/remove_note(collection, note) via `collection.notes`/ORM relationship append/remove + commit, and `list_notes(collection_id, page, size, *, user_id) -> tuple[list[Note], int]` joining `note_collections` filtered by collection_id and `Note.user_id == user_id` with `.options(selectinload(Note.tags))` and the count+offset/limit structure from notes repository. Add a `notes` relationship (or reuse note_collections) on Collection as needed for append/remove — define `notes: Mapped[list[Note]] = relationship(secondary=note_collections, lazy="select")` on the Collection model if append/remove is used (add to app/collections/models.py if missing).
    Create `app/collections/service.py`: CollectionService(repo: CollectionRepository, note_repo: NoteRepository). get_or_404_owned(collection_id, current_user) copying the notes 404/403 pattern. create, list_collections, add_note (own collection + own note, else 403/404), remove_note, list_notes returning the NoteListResponse envelope (pages = (total+size-1)//size if total else 0).
    Create `app/collections/router.py`: APIRouter(tags=["collections"]); POST "/" 201 CollectionRead; GET "/" list[CollectionRead]; POST "/{collection_id}/notes" 204 (body {"note_id": int}); DELETE "/{collection_id}/notes/{note_id}" 204; GET "/{collection_id}/notes" NoteListResponse with page/size Query. All depend on get_db + get_current_user.
    Add `get_collection_service` to dependencies.py mirroring get_note_service. Register in main.py: `app.include_router(collections_router, prefix="/collections")`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_collections.py -q 2>&1 | tail -5 && uv run ruff check app/collections app/main.py && uv run mypy app/collections</automated>
  </verify>
  <acceptance_criteria>
    - `app/collections/repository.py` contains `selectinload(Note.tags)` and a join on `note_collections`
    - `app/collections/service.py` raises 404 (missing) and 403 (wrong owner) for collection access
    - `app/main.py` registers `collections_router` with `prefix="/collections"`
    - GET /collections/{id}/notes returns a body with `items`, `total`, `pages` (NoteListResponse)
    - `uv run pytest tests/test_collections.py` exits 0; ruff + mypy clean on app/collections
  </acceptance_criteria>
  <done>ORG-03 + ORG-04 working end-to-end; collections isolated per user; test_collections.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → collections API | Untrusted body ({name}, {note_id}) and path params (collection_id, note_id) cross here |
| API → MySQL | ORM-managed note_collections writes; user_id scoping enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-iso | Elevation of Privilege | all /collections endpoints (IDOR on collection_id/note_id) | mitigate | get_or_404_owned on collection (403/404); add_note also verifies note ownership; list_notes filters `Note.user_id == user_id`; verified by test_collection_isolation |
| T-04-06 | Information Disclosure | GET /collections, GET /collections/{id}/notes | mitigate | list_by_user scopes to caller; note listing double-scoped by user_id |
| T-04-07 | Spoofing | all endpoints | mitigate | get_current_user JWT dependency (401) inherited from Phase 3 |
| T-04-SC | Tampering | dependency installs | accept | No new packages this phase |
</threat_model>

<verification>
- `uv run alembic upgrade head` applies 0004 then 0005
- `uv run pytest tests/test_collections.py -q` green; existing suites unaffected
- `uv run ruff check app/collections` and `uv run mypy app/collections` clean
- Manual (Swagger): create collection → add a note → GET /collections/{id}/notes shows it → DELETE removes it
</verification>

<success_criteria>
ORG-03 + ORG-04 met: collections CRUD + many-to-many note membership + per-user isolation (403/404), with the collection's notes returned in the canonical envelope.
</success_criteria>

<output>
Create `.planning/phases/04-tags-collections-full-text-search/04-03-SUMMARY.md` when done.
</output>
