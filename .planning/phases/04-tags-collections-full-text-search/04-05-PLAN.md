---
phase: 04-tags-collections-full-text-search
plan: 05
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - tests/test_phase4_isolation.py
autonomous: true
requirements: [ORG-01, ORG-02, ORG-03, ORG-04, SRCH-01]
user_setup: []

must_haves:
  decisions: "Validates D-02, D-07 (per-user isolation of tags and collections)."
  truths:
    - "User B cannot read or modify user A's tags (GET /tags, attach/detach on A's note)"
    - "User B cannot read or list user A's collections or their notes (403/404)"
    - "User B cannot add a note to user A's collection (403)"
    - "GET /search and GET /notes?tag= never return another user's notes"
    - "The full Phase-4 test suite is green before verification"
  artifacts:
    - path: "tests/test_phase4_isolation.py"
      provides: "consolidated cross-user isolation proof for tags, collections, search (success criterion 5)"
      min_lines: 60
  key_links:
    - from: "tests/test_phase4_isolation.py"
      to: "user_a_client / user_b_client"
      via: "existing conftest fixtures"
      pattern: "user_b_client"
---

<objective>
Provide the consolidated cross-user isolation proof for Phase 4 success criterion 5: user A can never read or modify user B's tags or collections, and search/tag-filter never leak across users. The per-slice plans each assert their own isolation; this plan is the single authoritative end-to-end isolation gate spanning all three resources, and confirms the whole Phase-4 suite is green.

Purpose: success criterion 5 (per-user isolation of tags AND collections) is the dominant security requirement (T-04-iso) and deserves a dedicated, exhaustive verification.
Output: `tests/test_phase4_isolation.py` and a green full suite.
</objective>

<phase_goal>
**As a** Second Brain user, **I want to** be certain my tags, collections, and searches are private to me, **so that** no other user can read or alter my organization or find my notes.

(Derived from success criterion 5 across ORG-01..04 and SRCH-01.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tags-collections-full-text-search/04-CONTEXT.md
@.planning/phases/04-tags-collections-full-text-search/04-PATTERNS.md
@.planning/phases/04-tags-collections-full-text-search/04-01-SUMMARY.md
@.planning/phases/04-tags-collections-full-text-search/04-03-SUMMARY.md
@.planning/phases/04-tags-collections-full-text-search/04-04-SUMMARY.md

<interfaces>
From tests/conftest.py: `user_a_client` and `user_b_client` AsyncClient fixtures (separate authenticated users) already exist — reuse, do NOT recreate.
Endpoints under test (built in prior plans): GET /tags, POST/DELETE /notes/{id}/tags; POST/GET /collections, POST/DELETE/GET /collections/{id}/notes; GET /notes?tag=; GET /search?q=.
Ownership contract: 403 if the resource exists but is owned by another user, 404 if it does not exist (Phase-3 contract).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidated cross-user isolation suite</name>
  <files>tests/test_phase4_isolation.py</files>
  <read_first>
    - tests/test_notes_isolation.py (complete cross-user 403/404 patterns)
    - tests/test_tags.py (test_tag_isolation from Plan 01)
    - tests/test_collections.py (test_collection_isolation from Plan 03)
    - tests/test_search.py (test_search_isolation from Plan 04)
    - .planning/phases/04-tags-collections-full-text-search/04-PATTERNS.md (test_phase4_isolation.py section 1036-1046)
  </read_first>
  <action>
    Create `tests/test_phase4_isolation.py` using the existing user_a_client/user_b_client fixtures. Assert, with real MySQL: (1) A creates a note and tags it "secret" — B's GET /tags does not contain "secret", and B cannot detach A's tag (DELETE /notes/{A_note}/tags/secret → 403/404); (2) A creates a collection — B's GET /collections does not list it, B's GET /collections/{A_id}/notes → 403, B's POST /collections/{A_id}/notes → 403, and a non-existent collection id → 404; (3) A creates a note containing a unique term — B's GET /search/?q={term} returns total 0, and B's GET /notes/?tag=secret returns no A notes. Keep assertions strict (no leakage, correct 403-vs-404). This is the binding proof of success criterion 5; it overlaps the per-slice isolation tests deliberately as the consolidated gate.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase4_isolation.py -q 2>&1 | tail -5 && uv run pytest -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_phase4_isolation.py` references both `user_a_client` and `user_b_client`
    - Covers tag isolation, collection isolation (403 exists / 404 missing), and search/tag-filter non-leakage
    - `uv run pytest tests/test_phase4_isolation.py` exits 0
    - `uv run pytest -q` (full suite) exits 0 — every Phase-4 and prior-phase test green
  </acceptance_criteria>
  <done>Success criterion 5 proven by a dedicated isolation suite; full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user A session → user B resources | All Phase-4 endpoints must refuse cross-user access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-iso | Elevation of Privilege / Information Disclosure | tags, collections, search, tag-filter | mitigate | This plan is the verification of the mitigations implemented in plans 01-04: per-user scoping + 403/404 ownership; asserted exhaustively across all three resources |
| T-04-SC | Tampering | dependency installs | accept | No new packages this phase |
</threat_model>

<verification>
- `uv run pytest tests/test_phase4_isolation.py -q` green
- `uv run pytest -q` full suite green (Phase-4 gate before /gsd:verify-work)
</verification>

<success_criteria>
Phase 4 success criterion 5 met and proven: all tag and collection resources are isolated per user; search and tag-filter never cross user boundaries; full suite green.
</success_criteria>

<output>
Create `.planning/phases/04-tags-collections-full-text-search/04-05-SUMMARY.md` when done.
</output>
